{% trans_default_domain 'ContactformBundle' %}
{% extends 'base.html.twig' %}

{% block title %}
	{% trans %}Contact{% endtrans %}
{% endblock %}

{% block body %}
	{% trans_default_domain 'ContactformBundle' %}
	<main class='svc-contactform' role='main'>

		<section aria-labelledby="contact-form-heading">
			<h2 id="contact-form-heading" class="sr-only">{% trans %}Contact{% endtrans %}</h2>
			{{ form(form, {'attr': {'novalidate': 'novalidate', 'aria-label': 'Contact form'}}) }}
		</section>
	</main>
{% endblock %}
